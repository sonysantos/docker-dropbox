#!/bin/bash

if [ "$1" == "" ]; then
  BASEDIR=$HOME
else
  BASEDIR=$1
fi

docker run -d --name my-dropbox \
  -v $BASEDIR/Dropbox:/dbox/Dropbox \
  -v $BASEDIR/.dropbox:/dbox/.dropbox \
  -e DBOX_UID=`id -u` \
  -e DBOX_GID=`id -g` \
  sonysantos/dropbox:latest
